<UserControl x:Class="odm.ui.controls.DeviceListPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="306" d:DesignWidth="231" xmlns:my="clr-namespace:odm.ui.controls" Unloaded="UserControl_Unloaded">	
    <my:ContentColumn x:Name="title">
        <my:ContentColumn.Footer>
            <Button x:Name="_refreshBtn" HorizontalAlignment="Right" Click="_refreshBtn_Click"  />    
        </my:ContentColumn.Footer>
        <!--<Border x:Name="border1" Margin="0,0,0,6" BorderBrush="Black" BorderThickness="1" CornerRadius="2"  >
            <DataGrid Margin="2"  AutoGenerateColumns="False" Name="_dataGrid" CanUserReorderColumns="False" HeadersVisibility="Column" IsReadOnly="True"/>
        </Border>-->
        <!--ItemsSource="{Binding Source=_devicesCollection}"-->
        <my:DeviceListControl x:Name="deviceList"/>
        <!--<ListBox Name="devicesList" ItemsSource="{Binding Source=_devicesCollection}" Background="Transparent" BorderThickness="0" ItemTemplate="{DynamicResource DeviceCardTemplate}">-->
        <!--</ListBox>-->
    </my:ContentColumn>
</UserControl>
