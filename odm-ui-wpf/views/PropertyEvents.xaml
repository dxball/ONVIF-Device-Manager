<Local:BasePropertyControl x:Class="odm.ui.controls.PropertyEvents"
             xmlns:Local="clr-namespace:odm.ui.controls"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             x:Name="root"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Local:ContentColumn x:Name="title" Background="{DynamicResource ResourceKey=PropertyBrush}">
        <DockPanel>
            <Image Name="screenshot" DockPanel.Dock="Top"/>
            <ListView ItemsSource="{Binding eventsCollection}">
                <ListView.View>
                    <GridView AllowsColumnReorder="true">
                        <GridViewColumn Header="eventId" DisplayMemberBinding="{Binding Path=EventID}" />
                        <GridViewColumn Header="dateTime" DisplayMemberBinding="{Binding Path=Date}" />
                        <GridViewColumn Header="type" DisplayMemberBinding="{Binding Path=Type}" />
                        <GridViewColumn Header="description" DisplayMemberBinding="{Binding Path=Details}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </DockPanel>
    </Local:ContentColumn>
</Local:BasePropertyControl>
